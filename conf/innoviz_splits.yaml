# Train / val splits for the Innoviz dataset, consumed by dataprocess/extract_innoviz.py.
# Sequences are listed as "<recording_day>/<sequence_name>" — matching the directory
# layout under --data-root (e.g. /mnt/data/lidar/processed/<recording_day>/<sequence_name>).
#
# Both lists must be non-empty when running the extractor in train+val mode. The CLI
# accepts --split train|val|both to control which lists are processed.

train: []

val: []
